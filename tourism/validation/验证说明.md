# 旅策协同 Travel.AI 4.2.0 验证说明

验证日期：2026-09-10。下列验证均未发起真实模型请求。

- 源码构建：`npm run build` 通过；该命令只从可发布源码生成 `dist/app`，不读取 `build-input/authorized-runtime.bin`。
- 程序回归：`npm test` 60 项通过、0 项失败，覆盖规则、schema、迁移、状态、Agent、模型配置、安全、品牌、Skill 和构建同步。
- 离线评测：25 个虚构固定案例、7 类，25/25 通过；见 `../evaluation/offline-evaluation-results.json` 和 `.md`。
- live runner：默认 dry-run，真实模型调用 0；必须显式提供 live、token 预算和请求上限才允许调用。本次未运行 live。
- 离线浏览器：隔离 Edge 共 26 项检查通过；含两大主场景、需求差异、中文表单、取消一致性、人工/自动方案并存和零 HTTP 业务请求，见 `browser-results.json`。
- 完整桌面：冷启动后 4.2.0 工作台、Agent 桥和 OpenClaw 网关就绪；标题、favicon、模型设置密码输入、空凭据回显和配置路径一致性通过，页面可读旧品牌命中 0，见 `desktop-results.json`。
- 可携版：从 ZIP 解压后使用随包 Node 启动，HTTP 200，页面版本与 Agent/模型桥正确；服务退出后端口释放。
- 源码包：从 ZIP 解压到干净目录后，源码构建、环境检查和 22 项源码包测试全部通过。
- 匿名与隐私：发布候选审计 0 命中；最终三类 ZIP 的文件名和文本条目再次扫描，旧品牌、学校身份、个人路径、电话、邮件与密钥模式均 0 命中。
- 打包：生成 source、portable-web、offline-demo 三类包；打包脚本排除不可发布构建输入、个人配置、日志、浏览器 Profile、开发过程文件和桌面二进制环境，并生成 SHA-256 manifest。

派生桌面 EXE SHA-256：`D3C5C9B062C24D6791A1229CF22415639C8A99FB6F3D127EE1BD0C0BE3CC4ECC`，签名状态 `NotSigned`。不可变构建输入 SHA-256 保持 `D0B395AFB78D50812570437C0AA52DEC1EBDDE2CFCE492750CB3D651BCC97337`，不参与参赛包发布。

三个 Skill 的结构、自包含脚本与职责边界由 Node 测试验证。skill-creator 的 Python `quick_validate.py` 因当前环境缺少 PyYAML 未能执行；未下载或安装依赖，现有测试对 frontmatter 键、名称、目录和脚本一致性执行了覆盖验证。

本验证只证明当前本地代码和虚构固定数据下的行为，不等同主办方验收，不代表实时资料、真实模型内容质量、转化率、收入或实训效果已验证。官方 PPT/文本模板和演示视频仍需参赛方在获得模板后制作并人工匿名检查。
