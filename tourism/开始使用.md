# 旅策协同：开发预览

双击项目根目录的 `启动旅策协同.vbs`，等待完整桌面工作台打开。关闭时从系统托盘选择“退出”；关闭窗口可能只会缩到托盘。

每次修改前端、Skill 或桌面运行代码后，先在项目根目录运行 `npm run build:desktop`，然后重新启动工作台查看最新成果。若启动失败，查看 `logs/launcher.log`。

演示数据均为虚构内容，需要在工作台中显式点击“载入演示”。模型和基础工具使用当前 Windows 用户目录中的开发配置；不要把 API Key 写入源码、任务 JSON 或截图。确定性评估和固定模板无需模型。
